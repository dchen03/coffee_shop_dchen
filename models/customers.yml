version: 2

models:
  - name: customers
    description: 'cleaned up table of customers'
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null

      - name: n_orders
        tests:
          - accepted_values:
              values: [0]
              quote: false